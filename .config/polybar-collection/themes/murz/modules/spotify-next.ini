[module/spotify-next]
type = custom/script
exec = echo "ﭠ"
format = <label>
click-left = playerctl next -p spotify
format-background = ${colors.bg}
format-foreground = ${colors.fg}
format-padding = 1
