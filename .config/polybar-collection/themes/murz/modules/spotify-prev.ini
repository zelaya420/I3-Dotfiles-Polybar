[module/spotify-prev]
type = custom/script
exec = echo "ﭢ"
format = <label>
click-left = playerctl previous -p spotify
format-background = ${colors.bg}
format-foreground = ${colors.fg}
format-padding = 1
